import React, { PropTypes } from "react";

const Header = (props) => {
    return (
        <header>
            <h1>Solstice web UI</h1>
        </header>
    );
};

Header.propTypes = {};

export default Header;
