import React, { PropTypes } from "react";

const Header = (props) => {
    return (
        <div id="header">
            <h1>Solstice web UI</h1>
        </div>
    );
};

Header.propTypes = {};

export default Header;
